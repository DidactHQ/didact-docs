# Deployments File

## Deployment Contexts

::: v-pre

- `${environment}`
The deployment's environment name.

```bash
${environment}
```

- `${deployment}`
The deployment's name.

```bash
${deployment}
```

- `${version}`
The deployment's version.

```bash
${version}
```

:::