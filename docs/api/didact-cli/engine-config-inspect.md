# Engine Config Inspect

Displays the configuration keys and values from Didact Engine's config file.

```bash-vue
didact engine config inspect
```

## Examples

Display the configuration keys and values from the config file.

```bash
didact engine config inspect
```