# UI Config Init

Initializes a new `uiconfig.json` file for Didact UI.

```bash-vue
didact ui config init
```

## Examples

Initialize a new config file for Didact UI.

```bash
didact ui config init
```