# UI Config Inspect

Displays the configuration keys and values from Didact UI's config file.

```bash-vue
didact ui config inspect
```

## Examples

Display the configuration keys and values from the config file.

```bash
didact ui config inspect
```