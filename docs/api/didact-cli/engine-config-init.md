# Engine Config Init

Initializes a new `engineconfig.json` file for Didact Engine.

```bash-vue
didact engine config init
```

## Examples

Initialize a new config file for Didact Engine.

```bash
didact engine config init
```